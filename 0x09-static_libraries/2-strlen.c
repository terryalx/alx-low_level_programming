#include "main.h"

/**
 * _strlen - function that returns the length of a string.
 * @s: Pointer input
 * Return: str
 */

int _strlen(char *s)
{
	int str;

	for (str = 0; s[str] != '\0'; str++)
	{
	}
	return (str);
}
